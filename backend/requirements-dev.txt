# Test-only dependencies — NOT installed in production.
# The pm2 venv (created by hadoku_site deploy) installs requirements.txt only.
# To run the suite locally:
#   python -m venv .venv && .venv/bin/python -m pip install -r backend/requirements.txt -r backend/requirements-dev.txt
#   cd backend && python3 -m pytest tests/ -v
pytest>=8,<10
pytest-asyncio>=0.24,<2
# Keep docs/hadoku-task-automation/openapi.json honest
# (tests/test_automation_openapi.py): jsonschema validates real responses
# against the schemas the document promises, and openapi-spec-validator checks
# the document itself against the OpenAPI 3.1 meta-spec. Test-only on purpose —
# the document is served as static bytes, so production needs neither.
jsonschema>=4.20,<5
openapi-spec-validator>=0.7,<1
