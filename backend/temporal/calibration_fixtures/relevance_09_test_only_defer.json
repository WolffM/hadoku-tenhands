{
  "rubric": "relevance_v1",
  "operator_verdict": "defer",
  "operator_score": 0.58,
  "notes": "The agent added a failing regression test that demonstrates the bug — but didn't fix it. Test infrastructure without the fix. on-topic 0.9 (right test file), focus 0.5 (test exists but no actual fix), completeness 0.2 (the bug isn't actually addressed). Borderline because the test is genuinely useful work the operator might choose to merge separately.",
  "input": "## Issue summary\n\nImage converter rotates EXIF-orientation-6 JPEGs incorrectly. Pictures shot in portrait on iPhone come out 90° clockwise rotated when extracted via the converter.\n\n## Files touched\n\n```\ntests/test_image_converter.py\ntests/fixtures/portrait_o6.jpg\n```\n\n## Diff\n\n```diff\ndiff --git a/tests/test_image_converter.py b/tests/test_image_converter.py\n--- a/tests/test_image_converter.py\n+++ b/tests/test_image_converter.py\n@@ -34,3 +34,18 @@ def test_basic_jpeg():\n+\n+import pytest\n+\n+@pytest.mark.xfail(reason=\"EXIF orientation handling not yet implemented\")\n+def test_exif_orientation_6_normalized():\n+    \"\"\"iPhone portrait — EXIF tag 6 = rotate 90 CW for display.\n+\n+    These tests are intentionally FAILING until the underlying EXIF\n+    handling is patched in image_converter.py.\n+    \"\"\"\n+    converter = ImageConverter()\n+    img = converter.open_image('tests/fixtures/portrait_o6.jpg')\n+    # Source is 4032x3024; with orientation applied → 3024x4032\n+    assert img.width == 3024 and img.height == 4032\n\ndiff --git a/tests/fixtures/portrait_o6.jpg b/tests/fixtures/portrait_o6.jpg\nnew file mode 100644\nindex 0000000..a1b2c3d\nBinary files /dev/null and b/tests/fixtures/portrait_o6.jpg differ\n```\n"
}
