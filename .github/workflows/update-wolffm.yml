name: Auto-update @wolffm packages

# NO `schedule:` TRIGGER HERE — deliberate, do not add one back.
#
# The daily safety net moved to hadoku_site's cron-worker, where a Temporal
# schedule fires ON TIME: `services/cron-worker/profiles.json` -> repo-sync
# profile (13:00 UTC / 06:00 Pacific, the last daily profile) -> tail job
# `ci:wolffm-fanout` -> `services/mgmt-api/src/routes/jobs/wolffm-fanout.ts`,
# which dispatches `wolffm_published` to every consumer in child-apps.json.
# That job throws on a non-204, so a dropped dispatch pages Discord — louder
# than this cron ever was, since a failed CI fan-out only emits a warning and
# leaves its run green.
#
# A GitHub Actions `schedule` is the wrong trigger for anything that ends in a
# deploy, because it does not fire when it says it does. Measured across 46
# runs of this same `0 */6 * * *` expression in hadoku-watchparty: median
# +57min late, max +162min, worst at the 00:00 UTC slot (median +105min)
# because midnight UTC is the most contended cron minute on the platform.
# 00:00 UTC is 17:00 Pacific — on 2026-08-10 that slot landed at 18:52 Pacific
# and redeployed production into a live watch party.
#
# Fast path is unchanged: a publish still dispatches `wolffm_published`
# immediately via hadoku_site's update-packages.yml fan-out.
on:
  repository_dispatch:
    types: [wolffm_published]
  workflow_dispatch:

concurrency:
  group: update-wolffm
  cancel-in-progress: true

permissions:
  contents: write
  packages: read

jobs:
  update:
    runs-on: ${{ fromJSON(vars.CI_RUNNER) }}
    timeout-minutes: 10
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Mark the start, so the reported duration is real
        run: echo "JOB_STARTED_AT=$(date -u +%s)" >> "$GITHUB_ENV"

      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          # The bot pushes straight to a protected main. The default GITHUB_TOKEN
          # is not an admin, so its push is rejected with "protected branch hook
          # declined" — and only when there is actually something to push, which
          # is why this can sit green for weeks. HADOKU_SITE_TOKEN is WolffM's
          # PAT and the branch has enforce_admins=false, so it bypasses. Same
          # pattern hadoku_site/update-packages.yml already uses.
          token: ${{ secrets.HADOKU_SITE_TOKEN }}

      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          # `registry-url` is what makes NODE_AUTH_TOKEN mean anything: without
          # it setup-node writes no auth file, so the env var on the steps below
          # was decorative and a separate step had to hand-write a .npmrc with
          # the PAT expanded into it in cleartext. With it, setup-node writes
          # the .npmrc to RUNNER_TEMP and points NPM_CONFIG_USERCONFIG at it,
          # referencing ${NODE_AUTH_TOKEN} rather than the value — so the token
          # stays in the environment and never lands in a file in the workspace.
          # publish.yml has always done it this way; this matches it.
          registry-url: https://npm.pkg.github.com
          scope: '@wolffm'
          cache-dependency-path: frontend/pnpm-lock.yaml

      # pnpm via corepack, pinned by the `packageManager` field in package.json.
      # Replaces pnpm/action-setup, whose self-installer shares one directory
      # across every runner instance on the host and dies with
      # `ENOENT ... uv_cwd` when concurrent jobs race it.
      - name: Enable pnpm
        run: |
          corepack enable
          corepack prepare pnpm@11.5.0 --activate
          pnpm --version

      # No "Configure .npmrc" step: setup-node above already wrote one, keyed to
      # ${NODE_AUTH_TOKEN}. The step that used to be here wrote the PAT itself
      # into frontend/.npmrc (this job defaults to working-directory: frontend),
      # which is the same file the repo now keeps as `${HADOKU_SITE_TOKEN}` —
      # a workspace file holding a live admin PAT for the length of the run,
      # one `git add -A` away from being committed. The commit step below names
      # its paths explicitly, so it never was, but the hazard did not need to
      # exist.

      - name: Update @wolffm/* to latest (majors included)
        env:
          NODE_AUTH_TOKEN: ${{ secrets.HADOKU_NPM_READ_TOKEN }}
        run: |
          pnpm --config.store-dir=${{ runner.tool_cache }}/pnpm-store update --latest "@wolffm/*"


      # Always-latest is the policy, so "not at latest" is a failure, not a note.
      # This runs AFTER the update above and re-reads the manifests, so it also
      # catches an update that silently did nothing (a lost dispatch, a registry
      # hiccup, a range pnpm declined to touch). The fan-out that triggers this
      # workflow is best-effort; this step is what makes a missed one visible.
      - name: Assert every @wolffm dependency is at latest
        env:
          NODE_AUTH_TOKEN: ${{ secrets.HADOKU_NPM_READ_TOKEN }}
        run: |
          node -e '
            const {execSync}=require("child_process"), fs=require("fs");
            const seen=new Set(); const stale=[]; const unchecked=[];
            const manifests=execSync("git ls-files \"*package.json\"").toString().trim().split("\n");
            for (const m of manifests) {
              if (!m || m.includes("node_modules")) continue;
              let pkg; try { pkg=JSON.parse(fs.readFileSync(m,"utf8")); } catch { continue; }
              for (const field of ["dependencies","devDependencies"]) {
                for (const [name,range] of Object.entries(pkg[field]||{})) {
                  if (!name.startsWith("@wolffm/")) continue;
                  // workspace: and * are not version claims — nothing to compare.
                  const r=String(range);
                  if (r.startsWith("workspace:") || r==="*") continue;
                  // Key by FIELD too, not just name+manifest: a package can sit in
                  // BOTH dependencies and devDependencies of one file with DIFFERENT
                  // ranges, and deduping by name alone checked whichever came first
                  // and silently skipped the stale one.
                  const key=name+"@"+m+"#"+field; if (seen.has(key)) continue; seen.add(key);
                  let latest; try {
                    latest=execSync(`npm view ${name} version --registry=https://npm.pkg.github.com`,{stdio:["ignore","pipe","ignore"]}).toString().trim();
                  } catch {
                    // NOT `continue`. An unreachable registry is indistinguishable
                    // from "at latest" if it is skipped, so a 401 here used to make
                    // the whole step report success while checking nothing — which
                    // is exactly what it happened to do wherever the .npmrc was not
                    // in this step working directory.
                    unchecked.push(`${name} in ${m}: could not read the registry`);
                    continue;
                  }
                  const declared=(r.match(/(\d+\.\d+\.\d+)/)||[])[1];
                  if (declared && latest && declared!==latest) {
                    stale.push(`${name} in ${m}: declares ${r}, latest is ${latest}`);
                  }
                }
              }
            }
            if (unchecked.length) {
              for (const u of unchecked) console.log(`::error::${u}`);
              console.error(`\n${unchecked.length} @wolffm package(s) could not be checked at all.`);
              console.error("Usually the .npmrc is not in this step working directory.");
              process.exit(1);
            }
            if (stale.length) {
              for (const s of stale) console.log(`::error::${s}`);
              console.error(`\n${stale.length} @wolffm dependency/ies are not at latest.`);
              console.error("Every @wolffm package must track the newest version, majors included.");
              console.error("If a major needs code changes, make them — do not pin back.");
              process.exit(1);
            }
            console.log("All @wolffm dependencies are at latest.");
          '
      - name: Commit and push if changed
        working-directory: ${{ github.workspace }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          if [ -z "$(git status --porcelain)" ]; then
            echo "✅ No @wolffm package updates available"
            exit 0
          fi

          git add frontend/package.json frontend/pnpm-lock.yaml
          git commit -m "chore: auto-update @wolffm/* to latest"

          for i in 1 2 3; do
            if git push; then
              echo "✅ Pushed"
              exit 0
            fi
            echo "Push failed (attempt $i), rebasing..."
            git fetch origin main
            git rebase origin/main || { git rebase --abort; exit 1; }
          done
          echo "❌ Push failed after retries"
          exit 1

      - name: Report the outcome so a silent failure can't hide
        # Why a job reports, and why this action is vendored rather than
        # shared, are both in .github/actions/report-job/action.yml.
        if: always()
        uses: ./.github/actions/report-job
        with:
          job-name: tenhands:update-wolffm
          service-key: ${{ secrets.KEY_SERVICE_TENHANDS }}
          started-at: ${{ env.JOB_STARTED_AT }}
          error-message: tenhands:update-wolffm failed — $RUN_URL
