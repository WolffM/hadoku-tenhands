// crimson-kitty (temporal pipeline) UI components.
//
// Components:
//   PipelineInbox     — operator inbox: issues awaiting human gates
//   IssueDetail       — per-issue view: timeline, evidence, gates, transitions
//   EvidencePreview   — renders evidence files inline (diffs, images, JSON)
//   StateBadge        — color-coded current-state badge
//   GateResultRow     — one row per gate result with verdict + reason
//
// See docs/crimson-kitty/pipeline-config.md for the design.
//
// Phase 1 deliverable. This file intentionally exports nothing yet.

export {}
