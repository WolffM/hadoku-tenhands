/**
 * The app's display name, injected by vite from @wolffm/catalogue at config time
 * (see vite.config.ts). Declared rather than imported: `define` is a literal text
 * substitution, so by the time this reaches the browser it is a string.
 */
declare const __HADOKU_APP_NAME__: string
