"""demotool — a tiny CSV report utility.

A deliberately small, honest project used as a dispatch demo target. It does
real work (parse a CSV, format dates, paginate rows, summarise a numeric
column) and ships with a passing test suite. A few edge-case bugs are seeded
on purpose for coding agents to find and fix; the happy paths all work.
"""

__version__ = "0.1.0"
